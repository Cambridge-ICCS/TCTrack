"""Unit tests for the machine-learning tracker."""

from tctrack.machine_learning import (
    MLParameters,
    MLTracker,
)


class TestMLParameters:
    """Tests for the MLParameters dataclass."""

    # Prevent ruff complaining about the unused import. Delete when no longer needed.
    parameters_class = MLParameters


class TestMLTracker:
    """Tests for the MLTracker class."""

    # Prevent ruff complaining about the unused import. Delete when no longer needed.
    tracker_class = MLTracker
